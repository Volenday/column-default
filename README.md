# @volenday/column-default

> React Table column for default.

## Install

Using npm:

```sh
npm install --save @volenday/column-default
```

or using yarn:

```sh
yarn add @volenday/column-default
```
